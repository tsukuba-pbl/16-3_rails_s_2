<%= form_for(@ft_layout) do |f| %>
  <% if @ft_layout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ft_layout.errors.count, "error") %> prohibited this ft_layout from being saved:</h2>

      <ul>
      <% @ft_layout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room_id %><br>
    <%= f.text_field :room_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
